PORT=5000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=educationdb

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# Frontend URL (for password reset links)
FRONTEND_URL=https://e-learning-project-five-lyart.vercel.app/

# Environment Mode
# Set to 'production' to send real emails
# Set to 'development' to show token on screen (testing mode)
NODE_ENV=production